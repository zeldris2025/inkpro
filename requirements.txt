# Core
Django==6.1.1
django-environ==0.14.0
djangorestframework==3.18.1
psycopg[binary]==3.2.3        # PostgreSQL driver (unused on SQLite)
whitenoise==6.8.2             # static file serving behind Gunicorn

# Documents & data
weasyprint==70.0              # branded quote PDFs (needs cairo/pango on the host)
openpyxl==3.1.5               # Recharge Register import
Pillow==12.3.0                # artwork uploads and thumbnails

# Async
celery==5.6.3
redis==8.1.0

# Production server
gunicorn==23.0.0
