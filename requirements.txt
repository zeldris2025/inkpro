# Core
# Pinned to the 5.2 LTS line (supported to April 2028) because Django 6.x
# requires PostgreSQL 15+, and the production database is on 14. Bump this
# once that server has been upgraded — nothing in the codebase uses a 6.x
# feature.
Django==5.2.17
django-environ==0.14.0
djangorestframework==3.18.1
psycopg[binary]==3.2.13        # PostgreSQL driver (unused on SQLite)
whitenoise==6.8.2             # static file serving behind Gunicorn

# Documents & data
weasyprint==70.0              # branded quote PDFs (needs cairo/pango on the host)
openpyxl==3.1.5               # Recharge Register import
pypdf==6.19.0                  # extracts the rate card's product photography
Pillow==12.3.0                # artwork uploads and thumbnails

# Async
celery==5.6.3
redis==8.1.0

# Production server
gunicorn==23.0.0
